@{
    Html.SetBodyClass("layout03 myGnoss");
    CommunityModel Comunidad = Html.GetComunidad();
    UserProfileModel Perfil = Html.GetPerfil();
    string urlInicio = "";

    if (Comunidad.Key.Equals(ProyectoAD.MetaProyecto))
    {
        urlInicio = Perfil.Url + Html.GetText("URLSEM", "HOME");
    }
    else
    {
        urlInicio = Comunidad.Url;
    }    
}
<div id="col02">
    <h2>@Html.GetText("CONFIRMACIONCAMBIOPASSWORD", "TITULONOUSUARIO")</h2>
    <div class="form formtheme01">
        <div class="box">
            <div class="fieldset">
                <fieldset class="labels">
                    <div class="fieldsetContent" id="panelContenido">
                        <p>
                            <span>@Html.GetText("CONFIRMACIONCAMBIOPASSWORD", "MENSAJENOUSUARIO")</span>
                        </p>
                        <p>
                            <a href="@urlInicio">@Html.GetText("CAMBIARPASSWORD", "IRINICIO")</a>
                        </p>
                    </div>
                </fieldset>
            </div>
        </div>
    </div>
</div>