@{
    Html.SetBodyClass("layout03 myGnoss");    
}
<div id="col02">
    <h2>@Html.GetText("CONFIRMACIONCAMBIOPASSWORD", "TITULOSOLICITADO")</h2>
    <div class="form formtheme01">
        <div class="box">
            <div class="fieldset">
                <fieldset class="labels">
                    <div class="fieldsetContent">
                        <p>
                            <span>@Html.GetText("CONFIRMACIONCAMBIOPASSWORD", "MENSAJESOLICITADO")</span>
                        </p>
                        <p>
                            @{
                                string urlInicio = Html.GetComunidad().Url;
                                if (Html.GetComunidad().Key.Equals(ProyectoAD.MetaProyecto))
                                {
                                    urlInicio = Html.GetPerfil().Url + Html.GetText("URLSEM", "HOME"); 
                                }
                            }

                            <a href="@urlInicio" >@Html.GetText("CAMBIARPASSWORD", "IRINICIO")</a>
                        </p>
                    </div>
                </fieldset>
            </div>
        </div>
    </div>
</div>
